 // deleteNode(head, tail, 2);
    // printNode(head);
    // cout << endl;

    // deleteNode(head, tail, 1);
    // printNode(head);
    // cout << endl;

    // deleteNode(head, tail, 1);
    // printNode(head);
    // cout << endl;