#include <bits/stdc++.h>
using namespace std;

int main()
{
   vector<int>arr(4, 0);

   for (int i = 0; i < arr.size(); i++)
   {
    cout<<arr[i]<<" ";
   }
   cout<<endl;

   cout<<"The size of the vector is:"<<arr.size();
   
   return 0;
}